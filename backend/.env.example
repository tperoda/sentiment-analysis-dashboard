# Copy to .env and fill in values
# No secrets required for MVP — model runs locally

# Optional: override HuggingFace cache dir
# HF_HOME=/tmp/hf_cache

# CORS origin for frontend (set to Vercel URL in production)
FRONTEND_ORIGIN=http://localhost:5173
